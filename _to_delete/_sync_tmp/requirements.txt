# The AI Incident Disclosure Shadow — Python dependencies. Python 3.12+.
#   pip install -r requirements.txt
#
# Versions are PINNED to the exact environment that produced the committed
# results (see environment.txt and the Dockerfile, and the audited Code Ocean
# capsule https://doi.org/10.24433/CO.2340354.v2). Pinning keeps a clean-machine
# reproduction bit-for-bit identical to the archived run.
#
# Core — needed to REPRODUCE every result, table, and figure from the frozen data
# (this is the only path most users need; it is offline and free):
numpy==2.2.6
scipy==1.14.1
matplotlib==3.9.2     # figures only
pytest==8.3.3         # runs the claim-verifier tests

# Data-gathering path only — NOT needed for reproduction. These drive the original
# AIID parse and point-in-time SEC EDGAR full-text search that produced the frozen
# coded table (data/disclosure_coding.csv):
# requests==2.32.3
# python-dateutil==2.9.0.post0
# tqdm==4.66.5
